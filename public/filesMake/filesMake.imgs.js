/**
 * 处理imgs文件夹
 * @param tree: 当前目录Json结构
 * @param dir: 当前目录路径
 * @param filesName: 当前文件名
 * @param fileInfo: 文件的信息
 */

const grunt = require('grunt');

const imgs = function(tree, dir, filesName, fileInfo){
    
};

module.exports = imgs;
