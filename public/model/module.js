import angular from 'angular';
import routerLink from 'routerLink';
import * as C from 'core';
[__IMPORT]

const [__NAME] = angular
    .module('[__NAME]', [[__DEPENDS]])
    [__INJECT]

export default [__NAME];
