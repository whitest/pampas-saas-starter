import angular from 'angular';
import routerLink from 'routerLink';
import * as C from 'core';
import 'angular-route';
[__IMPORT]

const [__NAME] = angular
    .module('[__NAME]', ['ngRoute', [__DEPENDS]])
    [__INJECT]

angular.bootstrap(document, [[__NAME].name])
